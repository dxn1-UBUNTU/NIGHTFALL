import type { UiState } from '../state.ts'; import { A } from '../ansi.ts';
export function render(s:UiState,w:number,h:number):string[]{return [A.bold+'Help'+A.reset,'','NIGHTFALL maps authorized web attack surfaces without automated credential attacks.','',A.cyan+'Target lifecycle'+A.reset,'  1. Add/authorize apex domain','  2. CT + optional DNS discovery','  3. Add JS/CSP/sitemap/HTML hostname clues','  4. Resolve and check every discovered host','  5. Crawl each live host separately','  6. Map routes/forms/auth surfaces + field selectors','  7. Run bounded canary analysis on testable query parameters','  8. Generate JSON + Markdown evidence reports','',A.cyan+'Keys'+A.reset,'  a target · d discover · Enter select · Tab next · j/k move · s save · q quit'].slice(0,h)}
